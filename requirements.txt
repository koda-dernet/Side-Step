# ╔══════════════════════════════════════════════════════════════╗
# ║  Side-Step — Standalone LoRA/LoKR Fine-Tuning Toolkit        ║
# ║  Install: pip install -r requirements.txt                    ║
# ╚══════════════════════════════════════════════════════════════╝
#
# CANONICAL SOURCE: pyproject.toml (install via `uv sync`).
# Default `uv sync` installs the **training/preprocess core** only.
# Match the old all-in-one bundle:  uv sync --extra full
#   (or: pip install -e ".[full]")
# Piecemeal: --extra gui --extra captions --extra audio_analyze --extra lycoris --extra optimizers --extra tui
#
# NOTE: uv is the recommended package manager (handles PyTorch
# CUDA wheels automatically). If using pip, install PyTorch with
# CUDA 12.8 first: https://pytorch.org/get-started/locally/

# ── Core ML ───────────────────────────────────────────────────
transformers>=4.51.0,<4.58.0
diffusers
lightning>=2.3.3
peft>=0.18.0
safetensors
einops>=0.8.1

# ── Vision (required by transformers AutoVideoProcessor) ─────
torchvision

# ── Audio loading & normalization ─────────────────────────────
torchcodec>=0.9.1
soundfile>=0.13.1
pyloudnorm>=0.1.0

# ── Model runtime ────────────────────────────────────────────
vector-quantize-pytorch>=1.27.15

# ── Logging / monitoring ─────────────────────────────────────
loguru>=0.7.3
tensorboard>=2.20.0

# ── CLI ───────────────────────────────────────────────────────
rich>=13.0.0

# ── AI sidecar (optional extra: captions) ───────────────────
# lyricsgenius, google-genai, openai, qwen-omni-utils

# ── Adapters / optimizers (optional: lycoris, optimizers) ─────
# lycoris-lora, bitsandbytes, prodigyopt

# ── Demucs stems (optional: audio_analyze); librosa is core ──
# demucs>=4.0.0
librosa>=0.10.0

# ── GUI (optional extra: gui) + native window (extra: native) ──
# mutagen, fastapi, uvicorn[standard], nvidia-ml-py, pywebview
# On Linux, also install Qt backend for native window:
#   pip install qtpy PyQt6 PyQt6-WebEngine
# On Windows, pywebview uses built-in Edge — no extras needed.

# ── Flash Attention 2 (optional — faster on Ampere+ GPUs) ────
# Requires CUDA compute capability >= 8.0 (RTX 3090+).
# With uv: installed automatically from prebuilt wheels.
# With pip: pip install flash-attn (may compile from source).
# flash-attn
